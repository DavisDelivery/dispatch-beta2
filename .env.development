# Local-dev defaults (loaded by Vite in `npm run dev` only).
# Plain `vite dev` does not run Netlify Functions, so default to mock mode so the
# Stops page renders the bundled fixture out of the box. Safe to commit — no secrets.
VITE_USE_MOCK_NUVIZZ=true
